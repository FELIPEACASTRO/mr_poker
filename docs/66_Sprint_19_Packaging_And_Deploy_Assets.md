# Sprint 19 — Packaging e Deploy Assets

Entregas implementadas:
- `Dockerfile`;
- `docker-compose.yml`;
- `.env.example`;
- configs `local` e `prod template`;
- workflow de testes para GitHub Actions.

Objetivo:
reduzir atrito entre laboratório local e futura operação controlada.
