# Sprint 11 — Dataset Builder e Splits

Entregas implementadas:
- builder de dataset unificado a partir de traces persistidos e spot packs;
- splits estáveis `train/validation/test` por hash do `source_key`;
- export em `jsonl` com `manifest.json` por dataset;
- contagens por split, source type e taxonomy.

Objetivo:
transformar o projeto de laboratório local em pipeline reproduzível de dados para treino, avaliação e distilação.
