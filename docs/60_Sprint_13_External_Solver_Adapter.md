# Sprint 13 — Adapter para Solver Externo

Entregas implementadas:
- catálogo de labels externos em `var/external_solver`;
- comparação de spot pack com label externo por `spot_id` ou `bucket_key`;
- sample file `sample_solver_v1.json`.

Objetivo:
abrir o caminho para substituir o `solver-like` heurístico por labels de solver real sem quebrar a arquitetura.
